{
  "name": "@recurly/engage-core",
  "version": "3.0.0",
  "description": "Recurly Engage core SDK",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "generate:version": "node version.js write",
    "build": "node version.js write && rm -rf dist && tsc",
    "pack": "npm pack",
    "test": "node version.js write && jest",
    "test:coverage": "node version.js write && jest --coverage"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "transform": {
      "^.+\\.tsx?$": [
        "ts-jest",
        {
          "tsconfig": {
            "types": [
              "jest"
            ],
            "noEmit": true
          }
        }
      ]
    }
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/recurly/recurly-engage-react-native-sdk-build.git"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/",
    "access": "public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "@types/node": "^22.13.4",
    "jest": "^29.7.0",
    "ts-jest": "^29.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
